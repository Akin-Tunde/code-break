# Game Logic & Testing Improvements

- Use per-position feedback for guesses to avoid coloring errors (especially with repeated digits).
- Add unit tests for core logic (evaluator, keyboard handlers, edge cases).
